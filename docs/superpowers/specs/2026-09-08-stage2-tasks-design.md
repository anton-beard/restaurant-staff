# Этап 2: задания, фото-отчёты и проверка через Claude. Дизайн

Дата: 2026-09-08. Статус: согласован с владельцем проекта. Уточняет и в части заданий заменяет разделы 3, 4, 5, 7 общего спека `2026-09-07-restaurant-staff-design.md`.

## 1. Цель и рамки

Владелец создаёт шаблоны заданий (разовые и регулярные), система выдаёт их сотрудникам в Telegram, сотрудники отчитываются кнопкой или фото, фото оценивает Claude, уверенно хорошие принимаются автоматически, остальные попадают владельцу на ручную проверку в боте и в админке.

Реальные задания этого ресторана, используются как тестовые данные и примеры в подсказках: «помыть кофемашину» (фото), «убрать стулья с улицы» (фото), «разобрать поставку кофе» (фото). Должности: Повар, Администратор, Бариста.

В рамки входит: шаблоны и расписания, режимы «каждому» и «одно на всех», экземпляры и статусы, сдача с фото (до 3 снимков), фоновая проверка Claude, очередь ручной проверки в боте и админке, напоминания и просрочки, хранение фото 90 дней, журнал заданий в админке, рефакторинг уведомлений и разбиение бота.

Не входит: курсы и тесты (этап 3), сводка и рейтинг (этап 4), учёт смен и графиков, Mini App.

## 2. Решения, принятые при обсуждении

| Вопрос | Решение |
|---|---|
| Выдача регулярного задания по должности | Владелец выбирает в шаблоне: `each` (каждому своя копия) или `shared` (одно на всех, берёт первый нажавший «Беру») |
| Формат расписания | По дням недели с набором времён, либо «каждые N минут с … до …» по дням недели. Cron наружу не показывается |
| Фото | До 3 снимков на одну сдачу, один вызов ИИ на все; флаг «требуется фото» в шаблоне, без него задание принимается кнопкой |
| Ручная проверка | И в боте (кнопки под уведомлением), и в админке (очередь) |
| Хранение фото | 90 дней (настройка `photo_retention_days`), файл удаляется, запись со счётом остаётся |
| Планировщик | Хранит `next_run_at` у шаблона; пропущенный слот старше 60 минут не выдаётся |
| Проверка ИИ | Фоном, не блокируя бота; статус в базе, зависшие перезапускает планировщик |
| Модель | `claude-sonnet-5` по умолчанию, переменная `AI_MODEL`; переключение на `claude-opus-5` без изменений кода |

## 3. Модель данных (миграция `002_tasks`)

- **task_templates**: id, title, description, requires_photo (0/1), photo_criteria (text, null если фото не нужно), auto_accept_threshold (int, по умолчанию 80), assignee_mode (`by_position` | `by_employees`), distribution (`each` | `shared`), schedule_kind (`once` | `weekly` | `interval`), schedule (json или null для `once`), deadline_minutes (int > 0), next_run_at (ISO или null), active (0/1), created_at.
  - `weekly`: `{ "days": [1..7], "times": ["10:00", "22:00"] }` (1 = понедельник, время локальное по `TZ`).
  - `interval`: `{ "days": [1..7], "from": "10:00", "to": "23:00", "every_minutes": 120 }`. Слоты: from, from+every, … пока ≤ to.
- **task_template_positions** (template_id, position_id), **task_template_employees** (template_id, employee_id).
- **task_instances**: id, template_id, employee_id (null пока общее не взято), slot_at (ISO), issued_at, due_at, claimed_at, status (`open` | `pending` | `submitted` | `review` | `accepted` | `overdue`), completed_at, reminder_sent_at. Индексы: уникальный (template_id, slot_at, employee_id) и уникальный частичный (template_id, slot_at) where employee_id is null для `shared`.
- **task_offers**: id, instance_id, telegram_id, message_id. Кому и каким сообщением предложено общее задание.
- **task_submissions**: id, instance_id, created_at, ai_status (`pending` | `done` | `failed`), ai_attempts (int), ai_score, ai_verdict, ai_issues (json-массив строк), decision (null | `auto_accepted` | `needs_review` | `owner_accepted` | `owner_rejected`), owner_comment, decided_at.
- **task_photos**: id, submission_id, position (1..3), path, telegram_file_unique_id (unique), deleted_at.
- **settings**: `photo_retention_days` (по умолчанию 90, если ключа нет).
- **bot_states** (есть): состояния `{"kind":"collecting_photos","instance_id":N,"photos":[...]}` для сотрудника и `{"kind":"reject_comment","submission_id":N}` для владельца.

Переходы статусов экземпляра:
- `open` → `pending` (нажал «Беру»), `open` → `overdue` (дедлайн).
- `pending` → `accepted` (без фото, кнопка «Выполнено»), `pending` → `submitted` (фото отправлены), `pending` → `overdue`.
- `submitted` → `accepted` (автоприём), `submitted` → `review` (needs_review или failed).
- `review` → `accepted` (владелец принял), `review` → `pending` (владелец отклонил; дедлайн не меняется, если он уже прошёл, следующий тик переведёт в `overdue`).
- `submitted` и `review` дедлайном не просрочиваются.

## 4. Бот

Модули: `bot/createBot.ts` (сборка, catch, fallback), `bot/linking.ts` (привязка и меню, перенос текущего кода), `bot/tasks.ts` (сотрудник), `bot/review.ts` (владелец), `bot/keyboards.ts`.

**Мои задания.** Список экземпляров сотрудника в статусах `pending`, `submitted`, `review`, каждое отдельным сообщением: название, дедлайн, статус, инлайн-кнопка «Открыть». Пусто: «Активных заданий нет».

**Карточка.** Описание, критерии (если фото), дедлайн. Кнопка «Выполнено» (без фото) сразу переводит в `accepted`. Кнопка «Отправить фото» ставит state `collecting_photos` и отвечает «Пришлите до 3 фото, потом нажмите Готово» с reply-клавиатурой «Готово» / «Отмена».

**Сбор фото.** Принимаются только сообщения типа photo (не документ), берётся самый большой размер, файл скачивается в `data/uploads/<instance_id>/<timestamp>-<n>.jpg`. Повтор `file_unique_id`, уже сохранённого в любой сдаче, отклоняется: «Это фото уже отправляли, снимите заново». Четвёртое фото: «Максимум 3 фото». «Готово» без фото: «Нужно хотя бы одно фото». «Отмена» удаляет скачанные файлы и снимает state. «Готово»: создаётся submission с `ai_status = pending`, экземпляр `submitted`, ответ «Проверяю, это займёт до минуты», запускается фоновая проверка. Итог сотруднику: «Принято, N из 100» или «Отправил владельцу на проверку». После решения владельца: «Принято» либо «Не принято: <комментарий>. Переснимите и отправьте снова» с кнопкой «Открыть».

**Общее задание.** При выдаче каждому подходящему сотруднику приходит сообщение с кнопкой «Беру», message_id пишется в `task_offers`. Первый нажавший: экземпляр `pending`, `employee_id` и `claimed_at` проставлены, его сообщение редактируется в карточку, у остальных текст меняется на «Взял(а) <имя>», кнопки убираются. Повторное нажатие после того, как взяли: всплывающий ответ «Уже взяли».

**Владелец.** `needs_review` или `failed`: альбом фото, затем текст (задание, сотрудник, оценка и замечания ИИ или «ИИ недоступен») с кнопками «Принять» / «Отклонить». «Отклонить» ставит state `reject_comment` и просит комментарий с кнопкой «Отмена». Решение по уже решённой сдаче: всплывающий ответ «Уже решено: <кем и когда>». Просрочки и «никто не взял» текстом. Кнопка «Сводка» дополняется числом заданий на проверке и просроченных за сегодня.

**Напоминания.** Одно на экземпляр: за 2 часа до дедлайна, если срок задания больше 4 часов, иначе за половину срока.

**Ошибки.** Любой сбой в хендлере: лог и «Что-то пошло не так, попробуйте ещё раз» (есть). Сбой отправки конкретному сотруднику (заблокировал бота) логируется и не останавливает рассылку остальным.

## 5. Проверка фото через Claude

Модуль `server/ai/photoReview.ts`:

```ts
type ReviewInput = { photos: { data: Buffer; mediaType: 'image/jpeg' | 'image/png' | 'image/webp' }[]; title: string; description: string; criteria: string }
type ReviewResult = { score: number; verdict: string; issues: string[] }
reviewPhotos(input, deps: { client: Anthropic; model: string }): Promise<ReviewResult>
```

- SDK `@anthropic-ai/sdk`, `client.messages.parse` со структурированным выводом `output_config.format = zodOutputFormat(schema)`, схема: score 0..100 (int), verdict (строка до 300 символов), issues (массив строк). `parsed_output === null` считается ошибкой.
- Модель из `AI_MODEL` (по умолчанию `claude-sonnet-5`), `thinking: { type: 'adaptive' }`, `output_config.effort = 'low'`, `max_tokens` 2000.
- Системный промпт постоянный, с `cache_control: { type: 'ephemeral' }`: роль проверяющего в ресторане, шкала 0–100 (100 = все критерии выполнены и видны; 80 = мелкие замечания; ниже 50 = существенные нарушения или по фото нельзя судить), правило «если критерий не виден на фото, снижай балл и называй это в issues», отвечать по-русски, кратко. Пользовательское сообщение: блоки image (base64) по числу фото, затем текст: название, описание, критерии.
- Таймаут запроса 60 с, `maxRetries` SDK по умолчанию (2). Исключение любого вида → результат отсутствует, `ai_status = failed`.
- Решение (`server/tasks/decide.ts`, чистая функция): `score >= threshold` → `auto_accepted`, иначе `needs_review`. Автоотказов нет.
- Фоновый запуск (`server/tasks/reviewQueue.ts`): очередь в памяти с параллелизмом 2; `enqueue(submissionId)`; воркер читает фото с диска, вызывает `reviewPhotos`, пишет результат, применяет решение, шлёт уведомления. `ai_attempts` увеличивается при каждом старте.
- Ошибки ИИ у владельца: пометка «ИИ недоступен, проверьте вручную», у сотрудника «Отправил владельцу на проверку».

## 6. Планировщик

Модуль `server/scheduler/`: `tick.ts` (вызывает шаги, ловит ошибки каждого отдельно), `schedule.ts` (чистые функции `slotsForDay(schedule, date)`, `nextRun(schedule, after: Date, tz: string): Date`), `issueTasks.ts`, `reminders.ts`, `overdue.ts`, `retryReviews.ts`, `cleanup.ts`. Тик раз в минуту через `setInterval` из `index.ts`, первый запуск через 5 секунд после старта; повторный вход, пока предыдущий тик не закончился, пропускается.

1. **Выдача.** Активные шаблоны с `next_run_at <= now`. Если `now - next_run_at > 60 минут`, слот пропускается (лог). Иначе выдача: `each` создаёт экземпляр `pending` каждому подходящему активному сотруднику (по должностям шаблона или поимённо; архивные пропускаются), `shared` создаёт один `open` и рассылает предложения. `due_at = slot_at + deadline_minutes`. Затем `next_run_at = nextRun(schedule, next_run_at)`. Уникальные индексы защищают от дублей при любом сбое посередине.
2. **Разовые** выдаются синхронно в API при создании шаблона; `next_run_at` у них null.
3. **Напоминания** по правилу из раздела 4.
4. **Просрочки**: `pending` и `open` с `due_at < now` → `overdue`, уведомления сотруднику (для `pending`) и владельцу.
5. **Зависшие проверки**: `ai_status = pending` и `created_at` старше 3 минут → повторный `enqueue`; при `ai_attempts >= 3` → `failed`, `needs_review`, уведомление владельцу.
6. **Чистка** (раз в час): фото старше `photo_retention_days` удаляются с диска, `deleted_at` проставляется; удаляются `owner_sessions` с истёкшим сроком и использованные `owner_login_codes`.

Смена должности или архивирование сотрудника не меняют уже выданные экземпляры.

## 7. Админка

Пункты меню: «Задания» (шаблоны и журнал), «Проверка фото» со счётчиком очереди.

**Шаблоны.** Таблица (название, расписание словами, исполнители, режим, фото, активен). Форма: название, описание, чекбокс «Требуется фото», критерии (видны при чекбоксе, плейсхолдер с примером про кофемашину), порог автоприёма (число, по умолчанию 80), исполнители (переключатель «по должностям» с чекбоксами / «поимённо» с чекбоксами по активным сотрудникам), режим `each`/`shared` с пояснением, расписание: переключатель `once`/`weekly`/`interval`; для weekly дни недели кнопками и список времён с «+»; для interval дни, «с», «до», «каждые N минут»; срок выполнения в минутах с пресетами 60, 240, 480. Валидация на клиенте и сервере (zod): хотя бы один исполнитель, хотя бы один день и одно время для регулярных, `from < to`, `every_minutes >= 15`. Сохранение `once` сразу выдаёт экземпляры и показывает «Выдано N сотрудникам» (для `shared` «Предложено N сотрудникам»). Кнопка «Остановить» ставит `active = 0`, «Возобновить» пересчитывает `next_run_at` от текущего момента.

**Журнал.** Таблица экземпляров с фильтрами (статус, сотрудник, шаблон, период), сортировка по выдаче. Карточка экземпляра: данные, список сдач с фото и оценками, решения.

**Проверка фото.** Очередь `needs_review` от старых к новым: фото крупно с переключением, задание и критерии, сотрудник, оценка и замечания ИИ или пометка «ИИ недоступен», поле комментария, кнопки «Принять» и «Отклонить» (отклонение требует непустого комментария). После решения карточка исчезает, счётчик обновляется.

**API** (все за сессией владельца):
- `GET/POST /api/tasks/templates`, `GET/PATCH /api/tasks/templates/:id`, `POST /api/tasks/templates/:id/deactivate`, `POST /api/tasks/templates/:id/activate`.
- `GET /api/tasks/instances?status&employee_id&template_id&from&to`, `GET /api/tasks/instances/:id` (со сдачами и фото).
- `GET /api/tasks/review-queue`, `POST /api/tasks/submissions/:id/decide` `{ decision: 'accept' | 'reject', comment? }` → 200 / 409 `already_decided`.
- `GET /api/uploads/*` отдаёт файлы из `data/uploads` только владельцу.

## 8. Уведомления и рефакторинг

`server/notify.ts` заменяется на `Notifier`:

```ts
type Notifier = {
  toOwner(text: string, extra?: { keyboard?: InlineKeyboard }): Promise<boolean>
  toEmployee(telegramId: number, text: string, extra?: { keyboard?: InlineKeyboard }): Promise<boolean>
  photosToOwner(paths: string[], caption: string, keyboard?: InlineKeyboard): Promise<boolean>
}
createTelegramNotifier(api: Api, db: Db): Notifier
```

`AppDeps.sendToOwner` заменяется на `notifier: Notifier`; API входа использует `notifier.toOwner`. Бот и планировщик получают тот же объект. Рефакторинг и разбиение бота идут первой задачей плана, до новой функциональности, с зелёными существующими тестами.

## 9. Конфигурация и деплой

- `ANTHROPIC_API_KEY` обязательна, `AI_MODEL` (по умолчанию `claude-sonnet-5`), `TZ` обязательна для расписаний (в Docker уже задана).
- `data/uploads` создаётся при старте.
- Зависимости: `@anthropic-ai/sdk`. Дата и часовые пояса через `Intl` без внешних библиотек.

## 10. Тестирование

- Чистые функции: `nextRun`/`slotsForDay` (границы суток, недели, интервалов, переход через полночь запрещён валидацией), решение по порогу, правило напоминания.
- Репозитории и планировщик: in-memory SQLite, подменённое время, идемпотентность повторного тика, пропуск старого слота, `shared` создаёт один экземпляр.
- API: inject, CRUD шаблонов с валидацией, выдача разового, очередь и решение, 409 при повторном решении, доступ к `/api/uploads` без сессии → 401.
- Бот: `handleUpdate` с захватом вызовов: список, «Выполнено», сбор фото (дубликат, лимит, отмена, готово), «Беру» первым и вторым, решение владельца в боте и комментарий.
- ИИ: мок клиента, проверка формы запроса (3 изображения + текст, модель из конфига), `parsed_output` null → ошибка, очередь с параллелизмом 2 и повтором зависших.
- Ручной прогон: шаблон «помыть кофемашину» с фото, три снимка, реальный ключ; один общий шаблон с двумя сотрудниками.

## 11. Порядок работ (для плана)

1. Рефакторинг: `Notifier`, разбиение бота, обязательный `ANTHROPIC_API_KEY`, `AI_MODEL`.
2. Миграция и репозитории заданий, функции расписания.
3. API шаблонов и выдача разовых; админка: шаблоны.
4. Бот сотрудника: список, карточка, «Выполнено», сбор фото, общее задание «Беру».
5. Claude: модуль проверки, очередь, решение, уведомления.
6. Владелец: решение в боте и API очереди; админка: проверка фото и журнал.
7. Планировщик: выдача регулярных, напоминания, просрочки, повтор зависших, чистка.
8. README и ручной прогон.
