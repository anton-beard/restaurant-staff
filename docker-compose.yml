services:
  app:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      DATA_DIR: /data
      PORT: "3000"
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
